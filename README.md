# Linux Modules

## Prerequisites

* a compiled kernel source tree
* `make modules` on that kernel source

## Common module-related commands

```bash
cat /proc/modules
lsmod
modprobe
insmod
rmmod
```
